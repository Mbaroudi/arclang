# Legacy examples

These files use syntax that the current compiler does not implement
(`=` attribute assignment, `import SI::{...}`, `security_policy`, and other
constructs from the unimplemented v2 specification), or predate the strict
parser. They are kept for reference and DO NOT compile.

Everything under `examples/` outside this directory is compiled in CI and
must stay green.
